export default class Pool {
  static name = 'pool';
  static aliases = [];
  static description = "Let's the user set his own champion pool";

  static run(bot, message, args, options) {
    message.channel.send('xd');
  }
}
